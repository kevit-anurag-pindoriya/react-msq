import React, { useEffect, useState } from "react";
export const selectedAnswerArray = [];

function SelectAnswer(props) {
    // const [key , setKey] = useState(0);
    // const [index , setIndex] = useState(0);
    // setKey(props.position);
    // setIndex(props.answer);
    // selectedAnswerArray[index] = key ; 
    // console.log("From Answer componets" + selectedAnswerArray);
    useEffect(()=>{});
  return (<>

  </>);
}

export default SelectAnswer;
